# Clon del repo del taller (tiene su propio .git, se obtiene con git clone)
taller-mba-uc/

# Entorno de Python del proyecto
.venv/

# Salidas de corridas: el contenido no se versiona, la carpeta si
runs/*
!runs/.gitkeep

# Cache de bytecode de Python
__pycache__/
*.pyc
